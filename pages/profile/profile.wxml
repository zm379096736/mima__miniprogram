<view class="screen">
  <view class="hero player-card">
    <view class="eyebrow">MY MIMA CARD</view>
    <view class="title">{{player.name}}</view>
    <view class="subtitle">{{player.tier}} 选手 / 偏好 {{positionText}}</view>
    <view class="card-score">{{player.score}}</view>
  </view>

  <view class="section grid-3">
    <view class="stat">
      <view class="stat-value">{{player.score}}</view>
      <view class="stat-label">积分</view>
    </view>
    <view class="stat">
      <view class="stat-value">{{player.wins}}</view>
      <view class="stat-label">胜场</view>
    </view>
    <view class="stat">
      <view class="stat-value">{{player.mvp}}</view>
      <view class="stat-label">MVP</view>
    </view>
  </view>

  <view class="section panel">
    <view class="section-title">我的资料</view>
    <view class="form-label">自评积分</view>
    <input class="input" type="number" value="{{form.score}}" bindinput="onScoreInput" placeholder="例如 85" />

    <view class="form-label">Steam ID</view>
    <input class="input" value="{{form.steamId}}" bindinput="onSteamInput" placeholder="选填，用于后续绑定战绩" />

    <view class="form-label">偏好位置</view>
    <checkbox-group bindchange="onPositionChange">
      <view class="position-grid">
        <label wx:for="{{positions}}" wx:key="value" class="{{item.className}}">
          <checkbox value="{{item.value}}" checked="{{item.checked}}" />{{item.label}}
        </label>
      </view>
    </checkbox-group>

    <button class="primary-btn" bindtap="saveProfile">保存我的卡片</button>
    <view class="subtitle">保存后，自动分队会立刻使用你的新积分和偏好位置。</view>
  </view>
</view>
